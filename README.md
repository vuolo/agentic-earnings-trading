# agentic-trading

Agent-driven trading experiments using the [Robinhood MCP trading server](https://agent.robinhood.com/mcp/trading).

## Setup

The Robinhood trading MCP server is configured in `.mcp.json`. Run `/mcp` in Claude Code to authenticate with Robinhood before trading.
